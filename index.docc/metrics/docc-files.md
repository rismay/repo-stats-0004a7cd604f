# `.docc` file trend

@Metadata {
  @PageKind(article)
  @TitleHeading(".docc file trend")
}

This view tracks cumulative `.docc` file count across Day windows.

Latest cumulative `.docc` files: 209

@Image(source: "index-daily-docc-file-chart-diagram", alt: "`.docc` file trend")
